<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="utils.RealmHelp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessRoutines"
        tools:targetApi="31">
        <activity
            android:name="activities.SplashActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name="activities.MainActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name="activities.EjerciciosActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name="activities.SliderActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name="activities.DiasActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name="activities.EjerciciosPersonalizadosActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name="activities.AllEjerciciosActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name="activities.SliderPersonalizadaActivity" android:windowSoftInputMode="adjustResize"/>

    </application>

</manifest>